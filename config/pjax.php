<?php

return array(

    /**
     * Opening and closing tag to denote the PJAX section to return.
     */
    'tag' => '{# PJAX #}',

    /**
     * We can also check whether their is a specific file for pjax. For example
     * instead of loading the file about.php we can load about-pjax.php.
     *
     * The value here is inserted between the filename and extension.
     */
    'file' => '-pjax',
);